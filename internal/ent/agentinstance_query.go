// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/looplj/axonhub/internal/ent/agent"
	"github.com/looplj/axonhub/internal/ent/agenthost"
	"github.com/looplj/axonhub/internal/ent/agentinstance"
	"github.com/looplj/axonhub/internal/ent/agentmessage"
	"github.com/looplj/axonhub/internal/ent/apikey"
	"github.com/looplj/axonhub/internal/ent/messagechannelagentinstance"
	"github.com/looplj/axonhub/internal/ent/predicate"
)

// AgentInstanceQuery is the builder for querying AgentInstance entities.
type AgentInstanceQuery struct {
	config
	ctx                             *QueryContext
	order                           []agentinstance.OrderOption
	inters                          []Interceptor
	predicates                      []predicate.AgentInstance
	withAgent                       *AgentQuery
	withHost                        *AgentHostQuery
	withAPIKey                      *APIKeyQuery
	withMessages                    *AgentMessageQuery
	withMessageChannelBindings      *MessageChannelAgentInstanceQuery
	loadTotal                       []func(context.Context, []*AgentInstance) error
	modifiers                       []func(*sql.Selector)
	withNamedMessages               map[string]*AgentMessageQuery
	withNamedMessageChannelBindings map[string]*MessageChannelAgentInstanceQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AgentInstanceQuery builder.
func (_q *AgentInstanceQuery) Where(ps ...predicate.AgentInstance) *AgentInstanceQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *AgentInstanceQuery) Limit(limit int) *AgentInstanceQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *AgentInstanceQuery) Offset(offset int) *AgentInstanceQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *AgentInstanceQuery) Unique(unique bool) *AgentInstanceQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *AgentInstanceQuery) Order(o ...agentinstance.OrderOption) *AgentInstanceQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryAgent chains the current query on the "agent" edge.
func (_q *AgentInstanceQuery) QueryAgent() *AgentQuery {
	query := (&AgentClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(agentinstance.Table, agentinstance.FieldID, selector),
			sqlgraph.To(agent.Table, agent.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, agentinstance.AgentTable, agentinstance.AgentColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryHost chains the current query on the "host" edge.
func (_q *AgentInstanceQuery) QueryHost() *AgentHostQuery {
	query := (&AgentHostClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(agentinstance.Table, agentinstance.FieldID, selector),
			sqlgraph.To(agenthost.Table, agenthost.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, agentinstance.HostTable, agentinstance.HostColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryAPIKey chains the current query on the "api_key" edge.
func (_q *AgentInstanceQuery) QueryAPIKey() *APIKeyQuery {
	query := (&APIKeyClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(agentinstance.Table, agentinstance.FieldID, selector),
			sqlgraph.To(apikey.Table, apikey.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, agentinstance.APIKeyTable, agentinstance.APIKeyColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryMessages chains the current query on the "messages" edge.
func (_q *AgentInstanceQuery) QueryMessages() *AgentMessageQuery {
	query := (&AgentMessageClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(agentinstance.Table, agentinstance.FieldID, selector),
			sqlgraph.To(agentmessage.Table, agentmessage.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, agentinstance.MessagesTable, agentinstance.MessagesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryMessageChannelBindings chains the current query on the "message_channel_bindings" edge.
func (_q *AgentInstanceQuery) QueryMessageChannelBindings() *MessageChannelAgentInstanceQuery {
	query := (&MessageChannelAgentInstanceClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(agentinstance.Table, agentinstance.FieldID, selector),
			sqlgraph.To(messagechannelagentinstance.Table, messagechannelagentinstance.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, agentinstance.MessageChannelBindingsTable, agentinstance.MessageChannelBindingsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first AgentInstance entity from the query.
// Returns a *NotFoundError when no AgentInstance was found.
func (_q *AgentInstanceQuery) First(ctx context.Context) (*AgentInstance, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{agentinstance.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *AgentInstanceQuery) FirstX(ctx context.Context) *AgentInstance {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AgentInstance ID from the query.
// Returns a *NotFoundError when no AgentInstance ID was found.
func (_q *AgentInstanceQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{agentinstance.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *AgentInstanceQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AgentInstance entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AgentInstance entity is found.
// Returns a *NotFoundError when no AgentInstance entities are found.
func (_q *AgentInstanceQuery) Only(ctx context.Context) (*AgentInstance, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{agentinstance.Label}
	default:
		return nil, &NotSingularError{agentinstance.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *AgentInstanceQuery) OnlyX(ctx context.Context) *AgentInstance {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AgentInstance ID in the query.
// Returns a *NotSingularError when more than one AgentInstance ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *AgentInstanceQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{agentinstance.Label}
	default:
		err = &NotSingularError{agentinstance.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *AgentInstanceQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AgentInstances.
func (_q *AgentInstanceQuery) All(ctx context.Context) ([]*AgentInstance, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*AgentInstance, *AgentInstanceQuery]()
	return withInterceptors[[]*AgentInstance](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *AgentInstanceQuery) AllX(ctx context.Context) []*AgentInstance {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AgentInstance IDs.
func (_q *AgentInstanceQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(agentinstance.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *AgentInstanceQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *AgentInstanceQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*AgentInstanceQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *AgentInstanceQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *AgentInstanceQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *AgentInstanceQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AgentInstanceQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *AgentInstanceQuery) Clone() *AgentInstanceQuery {
	if _q == nil {
		return nil
	}
	return &AgentInstanceQuery{
		config:                     _q.config,
		ctx:                        _q.ctx.Clone(),
		order:                      append([]agentinstance.OrderOption{}, _q.order...),
		inters:                     append([]Interceptor{}, _q.inters...),
		predicates:                 append([]predicate.AgentInstance{}, _q.predicates...),
		withAgent:                  _q.withAgent.Clone(),
		withHost:                   _q.withHost.Clone(),
		withAPIKey:                 _q.withAPIKey.Clone(),
		withMessages:               _q.withMessages.Clone(),
		withMessageChannelBindings: _q.withMessageChannelBindings.Clone(),
		// clone intermediate query.
		sql:       _q.sql.Clone(),
		path:      _q.path,
		modifiers: append([]func(*sql.Selector){}, _q.modifiers...),
	}
}

// WithAgent tells the query-builder to eager-load the nodes that are connected to
// the "agent" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *AgentInstanceQuery) WithAgent(opts ...func(*AgentQuery)) *AgentInstanceQuery {
	query := (&AgentClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withAgent = query
	return _q
}

// WithHost tells the query-builder to eager-load the nodes that are connected to
// the "host" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *AgentInstanceQuery) WithHost(opts ...func(*AgentHostQuery)) *AgentInstanceQuery {
	query := (&AgentHostClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withHost = query
	return _q
}

// WithAPIKey tells the query-builder to eager-load the nodes that are connected to
// the "api_key" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *AgentInstanceQuery) WithAPIKey(opts ...func(*APIKeyQuery)) *AgentInstanceQuery {
	query := (&APIKeyClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withAPIKey = query
	return _q
}

// WithMessages tells the query-builder to eager-load the nodes that are connected to
// the "messages" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *AgentInstanceQuery) WithMessages(opts ...func(*AgentMessageQuery)) *AgentInstanceQuery {
	query := (&AgentMessageClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withMessages = query
	return _q
}

// WithMessageChannelBindings tells the query-builder to eager-load the nodes that are connected to
// the "message_channel_bindings" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *AgentInstanceQuery) WithMessageChannelBindings(opts ...func(*MessageChannelAgentInstanceQuery)) *AgentInstanceQuery {
	query := (&MessageChannelAgentInstanceClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withMessageChannelBindings = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AgentInstance.Query().
//		GroupBy(agentinstance.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *AgentInstanceQuery) GroupBy(field string, fields ...string) *AgentInstanceGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AgentInstanceGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = agentinstance.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.AgentInstance.Query().
//		Select(agentinstance.FieldCreatedAt).
//		Scan(ctx, &v)
func (_q *AgentInstanceQuery) Select(fields ...string) *AgentInstanceSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &AgentInstanceSelect{AgentInstanceQuery: _q}
	sbuild.label = agentinstance.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AgentInstanceSelect configured with the given aggregations.
func (_q *AgentInstanceQuery) Aggregate(fns ...AggregateFunc) *AgentInstanceSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *AgentInstanceQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !agentinstance.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	if agentinstance.Policy == nil {
		return errors.New("ent: uninitialized agentinstance.Policy (forgotten import ent/runtime?)")
	}
	if err := agentinstance.Policy.EvalQuery(ctx, _q); err != nil {
		return err
	}
	return nil
}

func (_q *AgentInstanceQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AgentInstance, error) {
	var (
		nodes       = []*AgentInstance{}
		_spec       = _q.querySpec()
		loadedTypes = [5]bool{
			_q.withAgent != nil,
			_q.withHost != nil,
			_q.withAPIKey != nil,
			_q.withMessages != nil,
			_q.withMessageChannelBindings != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*AgentInstance).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &AgentInstance{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withAgent; query != nil {
		if err := _q.loadAgent(ctx, query, nodes, nil,
			func(n *AgentInstance, e *Agent) { n.Edges.Agent = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withHost; query != nil {
		if err := _q.loadHost(ctx, query, nodes, nil,
			func(n *AgentInstance, e *AgentHost) { n.Edges.Host = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withAPIKey; query != nil {
		if err := _q.loadAPIKey(ctx, query, nodes, nil,
			func(n *AgentInstance, e *APIKey) { n.Edges.APIKey = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withMessages; query != nil {
		if err := _q.loadMessages(ctx, query, nodes,
			func(n *AgentInstance) { n.Edges.Messages = []*AgentMessage{} },
			func(n *AgentInstance, e *AgentMessage) { n.Edges.Messages = append(n.Edges.Messages, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withMessageChannelBindings; query != nil {
		if err := _q.loadMessageChannelBindings(ctx, query, nodes,
			func(n *AgentInstance) { n.Edges.MessageChannelBindings = []*MessageChannelAgentInstance{} },
			func(n *AgentInstance, e *MessageChannelAgentInstance) {
				n.Edges.MessageChannelBindings = append(n.Edges.MessageChannelBindings, e)
			}); err != nil {
			return nil, err
		}
	}
	for name, query := range _q.withNamedMessages {
		if err := _q.loadMessages(ctx, query, nodes,
			func(n *AgentInstance) { n.appendNamedMessages(name) },
			func(n *AgentInstance, e *AgentMessage) { n.appendNamedMessages(name, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range _q.withNamedMessageChannelBindings {
		if err := _q.loadMessageChannelBindings(ctx, query, nodes,
			func(n *AgentInstance) { n.appendNamedMessageChannelBindings(name) },
			func(n *AgentInstance, e *MessageChannelAgentInstance) { n.appendNamedMessageChannelBindings(name, e) }); err != nil {
			return nil, err
		}
	}
	for i := range _q.loadTotal {
		if err := _q.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *AgentInstanceQuery) loadAgent(ctx context.Context, query *AgentQuery, nodes []*AgentInstance, init func(*AgentInstance), assign func(*AgentInstance, *Agent)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*AgentInstance)
	for i := range nodes {
		fk := nodes[i].AgentID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(agent.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "agent_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *AgentInstanceQuery) loadHost(ctx context.Context, query *AgentHostQuery, nodes []*AgentInstance, init func(*AgentInstance), assign func(*AgentInstance, *AgentHost)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*AgentInstance)
	for i := range nodes {
		if nodes[i].AgentHostID == nil {
			continue
		}
		fk := *nodes[i].AgentHostID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(agenthost.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "agent_host_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *AgentInstanceQuery) loadAPIKey(ctx context.Context, query *APIKeyQuery, nodes []*AgentInstance, init func(*AgentInstance), assign func(*AgentInstance, *APIKey)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*AgentInstance)
	for i := range nodes {
		fk := nodes[i].APIKeyID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(apikey.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "api_key_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *AgentInstanceQuery) loadMessages(ctx context.Context, query *AgentMessageQuery, nodes []*AgentInstance, init func(*AgentInstance), assign func(*AgentInstance, *AgentMessage)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*AgentInstance)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(agentmessage.FieldAgentInstanceID)
	}
	query.Where(predicate.AgentMessage(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(agentinstance.MessagesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.AgentInstanceID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "agent_instance_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *AgentInstanceQuery) loadMessageChannelBindings(ctx context.Context, query *MessageChannelAgentInstanceQuery, nodes []*AgentInstance, init func(*AgentInstance), assign func(*AgentInstance, *MessageChannelAgentInstance)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*AgentInstance)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(messagechannelagentinstance.FieldAgentInstanceID)
	}
	query.Where(predicate.MessageChannelAgentInstance(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(agentinstance.MessageChannelBindingsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.AgentInstanceID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "agent_instance_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *AgentInstanceQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *AgentInstanceQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(agentinstance.Table, agentinstance.Columns, sqlgraph.NewFieldSpec(agentinstance.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, agentinstance.FieldID)
		for i := range fields {
			if fields[i] != agentinstance.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withAgent != nil {
			_spec.Node.AddColumnOnce(agentinstance.FieldAgentID)
		}
		if _q.withHost != nil {
			_spec.Node.AddColumnOnce(agentinstance.FieldAgentHostID)
		}
		if _q.withAPIKey != nil {
			_spec.Node.AddColumnOnce(agentinstance.FieldAPIKeyID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *AgentInstanceQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(agentinstance.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = agentinstance.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_q *AgentInstanceQuery) Modify(modifiers ...func(s *sql.Selector)) *AgentInstanceSelect {
	_q.modifiers = append(_q.modifiers, modifiers...)
	return _q.Select()
}

// WithNamedMessages tells the query-builder to eager-load the nodes that are connected to the "messages"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (_q *AgentInstanceQuery) WithNamedMessages(name string, opts ...func(*AgentMessageQuery)) *AgentInstanceQuery {
	query := (&AgentMessageClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if _q.withNamedMessages == nil {
		_q.withNamedMessages = make(map[string]*AgentMessageQuery)
	}
	_q.withNamedMessages[name] = query
	return _q
}

// WithNamedMessageChannelBindings tells the query-builder to eager-load the nodes that are connected to the "message_channel_bindings"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (_q *AgentInstanceQuery) WithNamedMessageChannelBindings(name string, opts ...func(*MessageChannelAgentInstanceQuery)) *AgentInstanceQuery {
	query := (&MessageChannelAgentInstanceClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if _q.withNamedMessageChannelBindings == nil {
		_q.withNamedMessageChannelBindings = make(map[string]*MessageChannelAgentInstanceQuery)
	}
	_q.withNamedMessageChannelBindings[name] = query
	return _q
}

// AgentInstanceGroupBy is the group-by builder for AgentInstance entities.
type AgentInstanceGroupBy struct {
	selector
	build *AgentInstanceQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *AgentInstanceGroupBy) Aggregate(fns ...AggregateFunc) *AgentInstanceGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *AgentInstanceGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AgentInstanceQuery, *AgentInstanceGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *AgentInstanceGroupBy) sqlScan(ctx context.Context, root *AgentInstanceQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AgentInstanceSelect is the builder for selecting fields of AgentInstance entities.
type AgentInstanceSelect struct {
	*AgentInstanceQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *AgentInstanceSelect) Aggregate(fns ...AggregateFunc) *AgentInstanceSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *AgentInstanceSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AgentInstanceQuery, *AgentInstanceSelect](ctx, _s.AgentInstanceQuery, _s, _s.inters, v)
}

func (_s *AgentInstanceSelect) sqlScan(ctx context.Context, root *AgentInstanceQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_s *AgentInstanceSelect) Modify(modifiers ...func(s *sql.Selector)) *AgentInstanceSelect {
	_s.modifiers = append(_s.modifiers, modifiers...)
	return _s
}
